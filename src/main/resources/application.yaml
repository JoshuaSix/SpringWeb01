spring:
  application:
    name: SpringWeb

  dataSource:
    url: jdbc:postgresql://localhost:5432/josh
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true


